# boto3 is pre-installed in the AWS Lambda Python 3.12 runtime.
# Do NOT bundle it in the Lambda deployment zip.
# Listed here only for local development and testing.

boto3==1.35.0
botocore==1.35.0

# For local testing with mocked AWS services
moto[iam,s3]==5.0.0
pytest==8.3.0
requests